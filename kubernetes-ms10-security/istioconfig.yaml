apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: ms10-security
  namespace: msten-prod
spec:
  hosts:
  - "ms10-security.ms10.svc.cluster.local"
  gateways:
  - my-gateway
  http:
  - route:
    - destination:
        host: ms10-security.ms10.svc.cluster.local
        port:
          number: 80
